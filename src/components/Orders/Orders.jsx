import React from 'react';
import { useLoaderData, useNavigate } from 'react-router-dom';

const Orders = () => {

    // console.log(tshirts);
    return (
        <div>
            <h1>Your Orders</h1>
        </div>
    );
};

export default Orders;